export interface Student {
    id: number | string;
    firstname: string;
    lastname: string;
    email: string;
    subject: string; //this was harcoded list of values for some reason
    grade: number;
}